server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hify_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      database: 1

logging:
  level:
    com.hify: DEBUG
    com.baomidou.mybatisplus: DEBUG
