package com.hify.modules.mcp.domain;

/**
 * MCP module - Domain layer
 *
 * 职责：
 * - Service 接口实现（核心业务逻辑）
 * - MCP 工具调用封装
 * - MCP 消息协议处理
 * - Repository 接口定义
 * - 事务边界（@Transactional）
 * - 禁止直接依赖 Mapper，通过 Repository 接口
 * - 禁止依赖 web 层
 */
public class PlaceholderDomain {
    // Placeholder class for domain layer
}
