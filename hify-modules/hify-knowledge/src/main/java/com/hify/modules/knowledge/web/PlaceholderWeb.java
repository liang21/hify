package com.hify.modules.knowledge.web;

/**
 * Knowledge module - Web layer
 *
 * 职责：
 * - Controller（处理 HTTP 请求/响应）
 * - 参数校验（@Valid）
 * - 文档上传接口（MultipartFile）
 * - 调用本模块 api/ 接口获取服务
 * - 返回 Result<T> 统一响应格式
 * - 禁止直接调用其他模块的 domain/ 或 infra/
 * - 禁止直接操作数据库
 */
public class PlaceholderWeb {
    // Placeholder class for web layer
}
